{
  "name": "marketdeck-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/home.js && node --check scripts/build.mjs && node --check scripts/dev.mjs && npm test"
  },
  "engines": { "node": ">=20" }
}
